---
- name: Initial ubuntu mariadb
  hosts: ubuntu
  become: true
  vars_files:
    - ../vars/database.yaml

  tasks:
    # Tarea 1: Instala Mariadb y dependencias
    - name: Instalar Mariadb y dependencias
      ansible.builtin.apt:
        name: mariadb-server,python3-pymysql
        state: present
        update_cache: true

    # Tarea 2: Configura bind-address
    - name: Bind mariadb a cualquier interface
      ansible.builtin.lineinfile:
        path: /etc/mysql/mariadb.conf.d/50-server.cnf
        regexp: '^bind-address'
        line: 'bind-address = 0.0.0.0'
      notify: Reiniciar MariaDB

    # Tarea 3: Inicia MariaDB
    - name: Iniciar MariaDB
      ansible.builtin.systemd_service:
        name: mariadb
        state: started
        enabled: true

    # Tarea 4: Configura el cortafuegos para permitir solo la web
    - name: Permitir conexiones desde la máquina web solo en el puerto 3306
      community.general.ufw:
        port: 3306
        proto: tcp
        rule: allow
        source: "{{ hostvars['centos01']['ansible_host'] }}"

    # Tarea 5: Crear la base de datos
    - name: Crear la base de datos de la aplicación
      community.mysql.mysql_db:
        name: "{{ db_name }}"
        state: present
        login_unix_socket: /var/run/mysqld/mysqld.sock

    # Tarea 6: Copiar el script SQL
    - name: Copiar archivo SQL inicial al servidor
      ansible.builtin.copy:
        src: ../files/schema.sql
        dest: /tmp/schema.sql
        mode: '0600'

    # Tarea 7: Importar el esquema (idempotente)
    - name: Comprobar si la tabla 'cumpleanios' existe
      ansible.builtin.command: >
        mysql -u root -S /var/run/mysqld/mysqld.sock -N -s -e
        "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='{{ db_name }}' AND table_name='cumpleanios';"
      register: cumple_table_check
      changed_when: false

    - name: Importar el esquema inicial si falta la tabla 'cumpleanios'
      community.mysql.mysql_db:
        name: "{{ db_name }}"
        state: import
        target: /tmp/schema.sql
        login_unix_socket: /var/run/mysqld/mysqld.sock
      when: cumple_table_check.stdout | int == 0


    # Tarea 8: Insertar datos iniciales de cumpleaños
    - name: Insertar datos iniciales de cumpleanios
      community.mysql.mysql_query:
        login_unix_socket: /var/run/mysqld/mysqld.sock
        login_db: "{{ db_name }}"
        query: "INSERT IGNORE INTO cumpleanios (nombre, fecha) VALUES ('{{ item.nombre }}', '{{ item.fecha }}');"
      loop: "{{ cumpleanios_seed }}"
      when: cumpleanios_seed is defined

    # Tarea 9: Crear usuario de MariaDB
    - name: Crear usuario de MariaDB para el servidor de aplicación
      community.mysql.mysql_user:
        name: "{{ db_user }}"
        password: "{{ db_password }}"
        host: "{{ hostvars['centos01']['ansible_host'] }}"
        priv: "{{ db_name }}.*:ALL"
        state: present
        login_unix_socket: /var/run/mysqld/mysqld.sock
      notify: Reiniciar MariaDB

  handlers:
    - name: Reiniciar MariaDB
      ansible.builtin.systemd_service:
        name: mariadb
        state: restarted
